type: object
properties:
  error:
    type: string
    example: "Bad Request"
  error_description:
    type: string
    example: "The request could not be understood by the server due to malformed syntax."